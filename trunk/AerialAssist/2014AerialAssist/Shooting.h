#pragma once

/*
 * Uses the TorShooter object to call the jags and access the state machine.
 * Handles sensors to determine what state we are in.  Handles input to fire and so on.
 */

class Shooting {
  
};

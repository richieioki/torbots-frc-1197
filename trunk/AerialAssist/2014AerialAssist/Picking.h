#pragma once

/*
 * Uses the TorPicker object to call the jags and access the state machine.
 * Handles sensors to determine what state we are in.  Handles input to pickup and stuff.
 */

class Picking {
  
};
